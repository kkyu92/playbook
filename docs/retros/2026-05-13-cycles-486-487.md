# Retro: Cycles 486–487 (2026-05-13)

## 이번에 한 것

- **review-code** (cycle 486): `scan-promotions.mjs` disk 캐시 추가 (Gemini 임베딩 중복 호출 제거, `~/.develop-cycle-hub/cache/`) + `dashboard-update.mjs` SE Trigger-5 마커 설정 로직 버그 수정 (window 기산 오류 fix). SE Trigger-5 마커 skip 자기조정 성공 (명단 동일 7개 SE #120 대비 / SE 40% ≥ 40%).
- **curate** (cycle 487): wiki lint CLEAN (152 entries, 0 orphan/isolated/stale) + memory 갱신 (cycles 486-487 log) + CHANGELOG 0.5.78 + TODOS BRANCHED 42/15 갱신 + retro 박제.

## 잘된 것

- scan-promotions.mjs 캐시 도입으로 반복 Gemini 임베딩 API 호출 제거 — 실행 비용 절감.
- dashboard-update.mjs SE Trigger-5 마커 버그 수정 — 정확한 window 기산으로 신뢰성 향상.
- SE #120 이후 SE 마커 skip 자기조정 패턴 안정 유지 (명단 7개 동일 + SE 40%).
- wiki lint CLEAN 152 entries 유지 (orphan/isolated/stale 0건).

## 아쉬운 것

- BRANCHED 재발 지속 (local 42 / origin 15 ahead) — zero-touch push 정책 + 워커 auto-ingest 양방향 발산. R6 사용자 결정 대기.

## 다음에 적용할 것

- SE 명단 7개 안정 지속 — review-code/curate/worker-incident-triage 균형 유지.
- BRANCHED 해소 사용자 확인 요청 — git merge + push.

## Compound Assets

- CHANGELOG 0.5.78 박제
- memory: project_develop_cycle_skill_family (cycle 487/SE #120/batch 88/380 갱신)
- TODOS.md [P0] BRANCHED 42/15 갱신

## 수치

| 항목 | 값 |
|---|---|
| cycle | 487 (curate) |
| wiki entries | 152 (lint CLEAN) |
| N=380 batch | 88/380 |
| SE 누적 | 120회 (SE #120) |
| BRANCHED | local 42 / origin 15 |
| zero-touch | 64+→486+ cycle 검증 |
