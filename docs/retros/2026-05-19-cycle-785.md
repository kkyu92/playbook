# Retro: Cycle 785 (2026-05-19)

## 이번에 한 것

- **curate** (cycle 785): android-ai 10개 entry connections 경로 중복 수정 (80→0 orphan). `category/category/slug` → `category/slug` 일괄 수정 (79개 패턴). solutions `.md` 미인덱스 + 없는 retro entry 5건 제거. wiki lint CLEAN (198 entries). validate-content PASS. TODOS BRANCHED 갱신 (local 4 / origin 2). CHANGELOG 0.7.84.

## 잘된 것

- lint-content.mjs가 orphan 80건 명확히 탐지 → 근본 원인(category 중복) 즉시 파악.
- Python 스크립트로 10개 파일 79개 패턴 일괄 수정 — 수작업 불필요.
- CI failure 원인(BRANCHED + fix 미푸시)과 curate 영역(orphan)을 분리 진단.

## 아쉬운 것

- android-ai 10개 seed(958e071) 추가 시 connections 경로 검증이 없었음 → agent-dispatch 단계에서 lint 선행 없이 머지됨.
- BRANCHED 4/2 지속 — 로컬 fix 4개 커밋이 origin에 없어 CI 차단 계속.
- solutions `.md` 파일이 findMdxFiles에서 인덱싱 안 되는 구조적 제한 — connections 참조 불가 확인.

## 다음에 적용할 것

- agent-dispatch 일괄 seed 후 반드시 `pnpm lint` 선행 확인 (connections orphan 즉시 탐지).
- BRANCHED 해소 시 CI 자동 회복 (6f543b9 fix + 785 curate 포함).
- solutions `.md` → `.mdx` 전환 또는 findMdxFiles 확장 여부는 P3 deferred.

## Compound Assets

- solution 박제: `docs/solutions/llm-generation/2026-05-19-agent-seed-connection-path-doubling.md`
- CHANGELOG 0.7.84 박제
- TODOS.md BRANCHED 갱신 (cycle 785 실측 4/2 + CI 영향 명시)

## 수치

| 항목 | 값 |
|---|---|
| cycle | 785 (curate) |
| wiki entries | 198 (lint CLEAN) |
| orphan fix | 80→0 (79 중복 수정 + 5 제거) |
| BRANCHED | local 4 / origin 2 |
| zero-touch N | 28 |
